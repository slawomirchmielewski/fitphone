import 'package:flutter/material.dart';


class MeasurmentsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child:Center(
        child: Text("Don't have any measurments"),
      ),
    );
  }
}
